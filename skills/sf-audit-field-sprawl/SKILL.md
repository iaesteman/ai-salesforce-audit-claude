# Skill: sf-audit field-sprawl

Run a standalone Salesforce custom field sprawl audit on a live org.

## Activated by
`/sf-audit-field-sprawl [org-alias]`

## What This Skill Does

Identifies objects overloaded with custom fields (≥100 critical, ≥50 warning), fields untouched for 2+ years, and fields that appear to serve duplicate purposes on the same object.

Output file: `SF-FIELD-SPRAWL.md` (written to current directory)

---

## Phase 1: Connectivity Check

```bash
sf org display --target-org [org-alias] --json
```

If this fails, instruct the user: `sf org login web --alias [alias]`

Extract: Org Name, Username, Org ID, Edition, Instance URL.

---

## Phase 2: Run Field Sprawl Queries

```bash
# Top 25 objects by custom field count
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT EntityDefinitionId, EntityDefinition.QualifiedApiName, EntityDefinition.Label, COUNT(Id) fieldCount FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' GROUP BY EntityDefinitionId, EntityDefinition.QualifiedApiName, EntityDefinition.Label ORDER BY COUNT(Id) DESC LIMIT 25" \
  --json

# Objects with 100+ custom fields
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT EntityDefinitionId, EntityDefinition.QualifiedApiName, COUNT(Id) fieldCount FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' GROUP BY EntityDefinitionId, EntityDefinition.QualifiedApiName HAVING COUNT(Id) >= 100 ORDER BY COUNT(Id) DESC" \
  --json

# Objects with 50–99 custom fields
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT EntityDefinitionId, EntityDefinition.QualifiedApiName, COUNT(Id) fieldCount FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' GROUP BY EntityDefinitionId, EntityDefinition.QualifiedApiName HAVING COUNT(Id) >= 50 AND COUNT(Id) < 100 ORDER BY COUNT(Id) DESC" \
  --json

# Fields not modified in 2+ years (stale)
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT QualifiedApiName, EntityDefinition.QualifiedApiName, Label, LastModifiedDate, CreatedDate FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' AND LastModifiedDate < LAST_N_DAYS:730 ORDER BY LastModifiedDate ASC LIMIT 100" \
  --json

# Total custom fields
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT COUNT() FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c'" \
  --json

# All custom field names (for duplicate-purpose analysis)
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT QualifiedApiName, EntityDefinition.QualifiedApiName, Label FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' ORDER BY EntityDefinition.QualifiedApiName, QualifiedApiName LIMIT 1000" \
  --json
```

---

## Phase 3: Analyze & Score

**Objects ≥100 fields score:**
| Count |  Score  |
|:-----:|:-------:|
|   0   |   10    |
|  1–2  |    7    |
|  3–5  |    4    |
|  6–9  |    2    |
|  10+  |    0    |

**Objects 50–99 fields score:**
| Count |  Score  |
|:-----:|:-------:|
|   0   |   10    |
|  1–3  |    8    |
|  4–7  |    6    |
| 8–12  |    4    |
|  13+  |    2    |

**Stale fields:** unmodified >730 days / total × 100

| Stale Rate |  Score  |
|:----------:|:-------:|
|   < 10%    |   10    |
|  10–24%    |    8    |
|  25–39%    |    6    |
|  40–59%    |    4    |
|  60–79%    |    2    |
|   ≥ 80%    |    0    |

**Duplicate-purpose fields:** count objects with ≥2 fields sharing a semantic root

| Objects with duplicates |  Score  |
|:-----------------------:|:-------:|
|            0            |   10    |
|           1–2           |    8    |
|           3–5           |    6    |
|          6–10           |    4    |
|           11+           |    2    |

```
section_score = (
  critical_sprawl_score × 0.30 +
  stale_field_score     × 0.30 +
  warning_sprawl_score  × 0.20 +
  duplicate_score       × 0.20
) × 10
```

**Grade:**
|  Score  | Grade |
|:-------:|:-----:|
| 90–100  |  A+   |
|  80–89  |   A   |
|  70–79  |   B   |
|  60–69  |   C   |
|  50–59  |   D   |
|  < 50   |   F   |

---

## Phase 4: Write SF-FIELD-SPRAWL.md

```markdown
# Salesforce Custom Field Sprawl Report
**Org:** [name] | **Username:** [username] | **Edition:** [edition]
**Date:** [YYYY-MM-DD HH:MM UTC]
**Generated by:** /sf-audit-field-sprawl

---

## Score: [XX]/100 — Grade [X]

### Dimension Scores
|          Dimension          | Score  |                  Key Finding                  |
|:---------------------------:|:------:|:---------------------------------------------:|
| Objects ≥100 Custom Fields  | [X]/10 |      [n] objects over critical threshold      |
| Objects 50–99 Custom Fields | [X]/10 |          [n] objects in warning zone          |
|  Stale Fields (>730 days)   | [X]/10 |  [n] fields not modified in 2+ years ([x]%)   |
|  Duplicate-Purpose Fields   | [X]/10 | [n] objects with potentially duplicate fields |

### Field Density — Top 25 Objects
[table of top objects by field count]

### Objects at Critical Threshold (≥100 fields)
[table or "No objects exceed 100 custom fields."]

### Objects in Warning Zone (50–99 fields)
[table or "No objects in the 50–99 field warning zone."]

### Stale Fields — Top 10 Oldest
[table of oldest unmodified fields]

### Potential Duplicate-Purpose Fields
[table or "No obvious duplicate-purpose fields detected."]

### Recommendations
[Critical / Important / Best Practices]

---
*Run `/sf-audit` for a full org health audit across all domains.*
```

---

## Phase 5: Terminal Summary

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SF FIELD SPRAWL AUDIT — [Org Name]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Score:            [XX]/100 (Grade [X])
  Objects ≥100 fld: [n] (CRITICAL)
  Objects 50-99 fld:[n] (WARN)
  Stale fields:     [n] of [n] ([x]%)
  Duplicate fields: [n] objects flagged
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Report saved: SF-FIELD-SPRAWL.md
  Run /sf-audit [org] for full audit
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Output Standards
- Exclude managed package metadata from all counts
- For duplicate-purpose detection: compare lowercase stems after stripping `__c` and common suffixes. Flag only obvious matches
- Cap examples at 10 per category
- If 0 custom fields exist, score all dimensions at 10 and note it

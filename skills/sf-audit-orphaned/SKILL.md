# Skill: sf-audit orphaned

Run a standalone Salesforce orphaned metadata audit on a live org.

## Activated by
`/sf-audit-orphaned [org-alias]`

## What This Skill Does

Detects inactive, deactivated, and stale metadata cluttering the org: inactive flows, dead validation rules, deactivated workflow rules, and custom fields unmodified for 365+ days.

Output file: `SF-ORPHANED.md` (written to current directory)

---

## Phase 1: Connectivity Check

```bash
sf org display --target-org [org-alias] --json
```

If this fails, instruct the user: `sf org login web --alias [alias]`

Extract: Org Name, Username, Org ID, Edition, Instance URL.

---

## Phase 2: Run Orphaned Metadata Queries

```bash
# All flow definitions (active and inactive)
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT ApiName, Label, ProcessType, TriggerType FROM FlowDefinition ORDER BY ApiName LIMIT 500" \
  --json

# Inactive flows (no active version)
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT ApiName, Label, ProcessType FROM FlowDefinition WHERE ActiveVersion.VersionNumber = null ORDER BY ApiName LIMIT 200" \
  --json

# Total validation rules
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT COUNT() FROM ValidationRule" \
  --json

# Deactivated validation rules
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT Id, ValidationName, EntityDefinitionId, Description, Active FROM ValidationRule WHERE Active = false ORDER BY EntityDefinitionId LIMIT 200" \
  --json

# Total workflow rules
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT COUNT() FROM WorkflowRule" \
  --json

# Deactivated workflow rules
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT Id, Name, TableEnumOrId, Active FROM WorkflowRule WHERE Active = false ORDER BY TableEnumOrId LIMIT 200" \
  --json

# Custom fields not modified in 365+ days
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT QualifiedApiName, EntityDefinition.QualifiedApiName, Label, LastModifiedDate FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c' AND LastModifiedDate < LAST_N_DAYS:365 ORDER BY LastModifiedDate ASC LIMIT 200" \
  --json

# Total custom fields
sf data query --target-org [org-alias] --use-tooling-api \
  --query "SELECT COUNT() FROM FieldDefinition WHERE EntityDefinition.IsCustomizable = true AND QualifiedApiName LIKE '%__c'" \
  --json
```

---

## Phase 3: Analyze & Score

Apply violation rate → score table to each dimension:

| Violation Rate | Score |
|----------------|-------|
| < 5% | 10 |
| 5–14% | 8 |
| 15–29% | 6 |
| 30–49% | 4 |
| 50–74% | 2 |
| ≥ 75% | 0 |

**Inactive flows:** inactive / total flows × 100
**Dead validation rules:** inactive / total rules × 100
**Deactivated workflow rules:** deactivated / total rules × 100 (score 10 if 0 workflow rules)
**Stale custom fields:** unmodified >365 days / total fields × 100

```
section_score = (
  inactive_flow_score × 0.30 +
  dead_vr_score       × 0.25 +
  dead_wf_score       × 0.25 +
  stale_field_score   × 0.20
) × 10
```

**Grade:**
| Score | Grade |
|-------|-------|
| 90–100 | A+ |
| 80–89 | A |
| 70–79 | B |
| 60–69 | C |
| 50–59 | D |
| < 50 | F |

---

## Phase 4: Write SF-ORPHANED.md

```markdown
# Salesforce Orphaned Metadata Report
**Org:** [name] | **Username:** [username] | **Edition:** [edition]
**Date:** [YYYY-MM-DD HH:MM UTC]
**Generated by:** /sf-audit-orphaned

---

## Score: [XX]/100 — Grade [X]

### Dimension Scores
| Dimension | Score | Key Finding |
|-----------|-------|-------------|
| Inactive Flows | [X]/10 | [n] inactive flows out of [n] total ([x]%) |
| Dead Validation Rules | [X]/10 | [n] deactivated rules out of [n] total ([x]%) |
| Deactivated Workflow Rules | [X]/10 | [n] deactivated rules out of [n] total ([x]%) |
| Stale Custom Fields | [X]/10 | [n] fields unmodified >365 days ([x]%) |

### Inactive Flows
[table of inactive flows or "All flows have an active version."]

### Dead Validation Rules
[table of deactivated rules or "All validation rules are active."]

### Deactivated Workflow Rules
[table of deactivated rules or "All workflow rules are active." / "No workflow rules found."]

### Stale Custom Fields (Top 10)
[table of oldest unmodified fields]

### Recommendations
[Critical / Important / Best Practices]

---
*Run `/sf-audit` for a full org health audit across all domains.*
```

---

## Phase 5: Terminal Summary

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SF ORPHANED METADATA AUDIT — [Org Name]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Score:              [XX]/100 (Grade [X])
  Inactive flows:     [n] of [n] total ([x]%)
  Dead validation:    [n] of [n] total ([x]%)
  Dead workflows:     [n] of [n] total ([x]%)
  Stale fields:       [n] of [n] total ([x]%)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Report saved: SF-ORPHANED.md
  Run /sf-audit [org] for full audit
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Output Standards
- Exclude managed package metadata from all counts
- If a query fails (e.g., WorkflowRule not in edition), skip and note it
- Show specific names — never just counts
- Cap examples at 10 per category
